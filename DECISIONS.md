# Decisions Register (append-only)

## [2026-07-06] Live-agent initiative — three-phase direction (LIVE.1 = Phase A)
**Context:** User wants LifeDash to shift from post-hoc pipeline (record → review later) to a live experience: an AI partner during recording, evolving toward Graphify-style structured knowledge (entities + typed edges + confidence tags, queryable by the agent).
**Decision:** Three sequential phases: A) Live Assistant (drawer with live transcript + on-demand agent chat with tools), B) ambient auto-triage feed (proactive suggestions, one-tap accept), C) knowledge-graph substrate (entity/edge tables in PGlite, extraction post-meeting first). LIVE.1 covers A only.
**Alternatives:** Building the knowledge graph first (rejected — most experimental, needs the live surface to be useful); one mega-plan for all three (rejected — exceeds 6-task limit, bundles unrelated smoke tests).
**Rationale:** Each phase validates the next; A reuses proven infra (streaming transcription, card-agent tool loop) so it's the cheapest slice that changes the product's feel.

## [2026-07-06] "Live Assistant" naming (not "Copilot")
**Context:** User asked whether "copilot" meant Microsoft Copilot — the term is now trademark-loaded and could read as a Microsoft integration.
**Decision:** User-facing name "Live Assistant"; internal naming `meeting_agent` (schema, service, IPC), task type `live_assistant`.
**Alternatives:** "Meeting Partner", "Live Intelligence", keeping "Copilot".
**Rationale:** Avoids brand confusion in a privacy-positioned product; "Live Assistant" is descriptive and pairs with the existing "Meeting Intelligence" naming.

## [2026-07-06] Local-first model policy for the Live Assistant
**Context:** Privacy is important but not a hard product line; user runs LM Studio with hardware for up to 14B models. Research (2026-07-06) tiered local models and ranked API privacy postures.
**Decision:** Live Assistant defaults to the user-configured local provider (LM Studio, Qwen3-14B class recommended) via the existing `resolveTaskModel` per-task routing — no new provider infra in Phase A. Cloud fallback stays user-chosen per task; future work may add Mistral/Groq and provider privacy badges.
**Alternatives:** Dual-model local tiering now (4B triage + 14B chat — deferred to Phase B where the triage loop exists); adding privacy-ranked cloud providers now (deferred, tracked in STATE.md pending items).
**Rationale:** Phase A is on-demand only, so one 14B model suffices; the per-task routing already expresses "local by default, escalate by choice". Research: complex KG relationship extraction (Phase C) exceeds 14B-class local models — revisit there.

## [2026-07-06] Live transcript state moves to recordingStore; drawer is a global portal
**Context:** The only `onTranscriptSegment` subscriber is MeetingsModern, so live segments are invisible on other views; child overlays hit a known stacking-context trap (standup picker precedent).
**Decision:** Single store-level transcript subscription (recordingStore) consumed by both MeetingsModern and the new LiveMeetingDrawer; drawer renders via `createPortal(document.body)` from AppLayout, only while recording.
**Alternatives:** Dedicated live-meeting route (rejected by user — leaves the Kanban); per-component IPC subscriptions (rejected — duplicate listeners, lost segments off-view).
**Rationale:** The user's core ask is working in Kanban while the meeting runs; one subscription avoids drift between surfaces.

## [2026-07-06] Live Mode pivot — recording becomes a full-screen takeover (supersedes the drawer decision)
**Context:** After LIVE.1 implementation, the user rejected the drawer positioning: "while the recording is happening nothing else should count at that moment... the live assistant should perform all tasks for me." The drawer optimized for multitasking the target user doesn't do mid-meeting.
**Decision:** LIVE.2 replaces the drawer with `LiveModeOverlay` — full-screen (FocusOverlay pattern), auto-enters on recording start, transcript spine + proposals feed + chat. Minimize collapses to the recording pill (deliberate escape hatch, not a hard lock; Esc minimizes, never stops). Drawer shell retired; its content components (transcript feed, chat) are recomposed inside the overlay. recordingStore transcript state carries over unchanged.
**Alternatives:** Hard navigation lock during recording (rejected — when the assistant can't do something, a locked UI turns a small gap into rage); keeping drawer as a middle state (rejected — three UI states is too many).
**Rationale:** For a meeting-intelligence-first product, the meeting is the main event; "LifeDash becomes the meeting" is the stronger product statement, and the expanded toolset makes leaving unnecessary rather than forbidden.

## [2026-07-06] Proactive proposals: propose → one-tap accept (not auto-execute)
**Context:** LIVE.2 pulls the ambient triage loop (old Phase B) forward. MEET-INTEL.1 precedent favored auto-execute+undo, but that was post-meeting batch output of a proven extraction; live triage runs a local 14B on noisy in-flight transcript.
**Decision:** Detected items (action_item/decision/question) land in `live_suggestions` as chips with one-tap Accept/Dismiss. Accepting an action_item creates the card through the live-assistant rails; decisions/questions become brief context. Auto-execute may graduate later as a settings flip. Accepted live items are suppressed from the post-meeting extractor to prevent duplicate cards.
**Alternatives:** Auto-execute + undo (rejected for v1 — noisy transcript + unproven local-model precision could spray the Inbox); hybrid by confidence (rejected — local-model confidence is poorly calibrated; two code paths for v1).
**Rationale:** Trust graduates with evidence; the propose model costs one tap and protects the Inbox while 14B precision is unproven.

## [2026-07-06] One local model serves chat + triage, chat priority (task type `live_triage`)
**Context:** User hardware runs one ~14B model in LM Studio alongside whisper.cpp; LM Studio serializes concurrent requests to a loaded model.
**Decision:** `live_triage` is a distinct task type whose model resolution falls back to `live_assistant`'s — same model by default, splittable in Settings later. Triage is cadence-gated (≥4 new segments ≈ 40s), skips (never queues) when a chat stream is in flight, caps 3 proposals per run, and degrades to "fewer proposals" on JSON failure (retry once, then skip — never throw into the recording pipeline).
**Alternatives:** Loading a second small model (Qwen3-4B) for triage now (rejected — VRAM contention with 14B + whisper unverified; the task-type split keeps this a settings change later); fixed timer instead of segment-count cadence (rejected — ties triage to wall-clock instead of actual speech volume).
**Rationale:** Chat is the user-facing interaction and must stay responsive; triage is background work that tolerates postponement.

## [2026-07-06] LIVE.2 close-out (Task 6): shared chip rendering, prop-driven post-meeting load, real-pipeline anti-dup test
**Context:** Final LIVE.2 task needed a post-meeting surface for un-actioned proposals, the `'live-assistant'` provenance badge, release notes, and a test proving the accepted-live-item anti-dup contract (Task 2) actually prevents a duplicate card, not just a prompt string.
**Decision:** (1) Extracted the chip/confirmation markup out of `LiveProposalsFeed` into a new presentational `LiveProposalChips` component; `LiveProposalsFeed` (store-driven, live) and the new `meeting-detail/LiveProposalsSection` (prop-driven, loads via `listLiveSuggestions` independently of `liveSuggestionsStore` since that store clears on stop) both render through it — one markup, two data sources. (2) `'live-assistant'` added to the `CardSource` union; its `KanbanCardModern` badge mirrors `'auto-from-meeting'`'s structure exactly (badge + tooltip + `handleViewSource` click) but is a separate conditional block — it intentionally does NOT gate the Reject/Keep-in-Inbox overflow menu, since that menu is specific to the auto-push review flow and doesn't apply to live-assistant cards. (3) The anti-dup integration test does NOT mock `autoPushService` — it runs the real `generateActionItems` → `autoPushActionItems` round trip against a table-identity-routed DB double, asserting on the actual `cards` insert call (exactly one card, not matching the suppressed title) rather than only on the LLM prompt string. This is a stronger proof than Task 2's own tests, but still assumes LLM compliance with the suppression instruction — there is no code-level title de-dup filter, by original Task 2 design. (4) Corrected the stale "Live Assistant drawer" release-note bullet (pre-dated the Task 4 full-screen pivot) to describe Live Mode instead of leaving factually wrong un-released copy.
**Alternatives:** A wholly separate hand-rolled list component for the meeting-detail section (rejected — direct markup duplication, against the story's "reuse, don't rebuild" instruction); keeping the anti-dup test mocked at the `autoPushActionItems` call-args level only (rejected — proves plumbing, not that a duplicate card is actually withheld); adding a code-level title-matching filter in `generateActionItems` to make suppression a hard guarantee (rejected — out of scope for Task 6, which only adds tests/UI per the story; would be a Task 2 design change requiring its own sign-off).
**Rationale:** Reuse over rebuild kept the two proposal surfaces from drifting apart; asserting on real DB-insert calls makes the anti-dup test meaningful rather than tautological; leaving the prompt-only suppression mechanism alone respects the already-locked Task 2 contract instead of silently expanding this task's scope.

## [2026-07-06] LIVE.3 scope + method — field-test fixes and project-proposal triage; Fable plans, Opus ultracode executes
**Context:** First real-meeting test of Live Mode (LIVE.2, uncommitted) surfaced three things: (A) the overlay chat is not clickable (static root-cause hunt across handlers, z-stack, scanlines, ConfirmDialog, the z-[100] pointer-events-none HUD beam, and the 36px TitleBar drag strip found no covering element); (B) high GPU during recording; (C) the user does not want to chat mid-meeting, so the assistant should be able to create a project itself from the conversation.
**Decision:** Bundle all three into a LIVE.3 plan (6 tasks). (1) Clickability fix is EMPIRICAL-FIRST — reproduce via `document.elementsFromPoint` in the running app before touching code; when the fix must be produced headless, apply the justified defensive union (raise the overlay above app chrome z-index + `isolation: isolate` + `WebkitAppRegion:'no-drag'` on the overlay root) and confirm via the user's manual smoke test. (2) GPU is split into inherent-vs-fixable: the dominant cost (whisper.cpp GPU + the 14B LM Studio model + triage cadence) is INHERENT to the 100%-local design and explicitly out of scope; the fixable renderer waste is AudioLevelMeter's never-settling 60fps canvas rAF (Math.random per frame) plus a second meter instance running behind the overlay — park the loop at silence, cap ~30fps, render only one meter; plus a main-process lever: triage yields to in-flight transcription via a one-way injected busy-probe (no new import cycle). (3) Project creation uses the propose→one-tap-accept model, NEVER auto-create: a new `live_suggestions` type `'project'` (+ `acceptedProjectId`, migration 0038); triage proposes at most one "Create project: {name}" chip when the meeting is unlinked; accept creates the project and links the meeting (reusing meetingService.updateMeeting, which already auto-pushes pending action items), rerouting future accepted items into it; a secondary `createProject` agent tool covers the typed-chat path.
**Method:** Planned with Fable (claude-fable-5); executed with Opus 4.8 + ultracode (multi-agent Workflow), per explicit user direction. Tasks run in dependency order (clickability + the two perf tasks are independent; schema 0038 → project rails → triage/UI feature slice must be sequential). Runtime acceptance rests on the user's manual smoke test per project convention.
**Alternatives:** Auto-create + link projects silently (rejected — a 14B on noisy transcript could spawn spurious projects, harder to undo than a card); shotgun z-index changes for the click bug (rejected — guess-fix without evidence); claiming the GPU is "fixable" wholesale (rejected — dishonest; the dominant cost is inherent local inference); post-meeting-only project prompt (rejected — doesn't solve the mid-meeting ask).
**Rationale:** The click bug blocks all other Live Mode verification, so it goes first and must be evidence-driven. Honesty about inherent GPU protects trust while still removing the real renderer waste. The propose→accept project chip extends the already-proven LIVE.2 model with the least new risk and directly answers "I can't chat while people are talking to me."

## [2026-07-07] v3 direction — session-centric app, living graph, digital twin (vision-level; roadmap pending)
**Context:** /nexus-discussion 2026-07-07 — after LIVE.2/3, user redefined the product: recording sessions become the app's center; Projects/Brainstorm/Ideas/Focus stop being destinations; the session is a workspace where the assistant's actions are watched live (embedded Kanban, growing information graph — "a living brain"); the assistant becomes a digital twin with a profile-creation phase and continuous learning.
**Decision:** Four-plan sequence on top of committed LIVE.3 (5b9586f): V3.1 session workspace + IA collapse (sessions home, switchable canvas Transcript | Board | Brain, activity feed, legacy sections hidden-not-deleted); V3.2 living graph v1 — STRUCTURAL (existing schema relations animated live; no NLP entity extraction yet); V3.3 twin creation phase (profile interview + prompt injection into assistant/triage/brief); V3.4 twin learning loop + graph semantic enrichment (early Phase C). Gate: the LIVE.2/3 real-meeting smoke test runs BEFORE v3 planning — v3's centerpiece (visible live agent actions) depends on the still-unverified LM Studio tool loop.
**Scope answers from user (2026-07-07):** (1) NO separate Library view — the sessions list IS the browse surface; boards are seen only through sessions. Search in the Meetings space over sessions/projects, semantic (RAG) desired: sequencing = plain Postgres full-text search in V3.1 (zero new infra), pgvector + local embedding model (LM Studio-served) rides with the graph phase. (2) Twin learned facts apply IMMEDIATELY (no approval queue) — requires the audit ledger with per-fact provenance, one-tap forget, and a global pause-learned-facts kill-switch as hard requirements.
**Alternatives:** Approval queue for learned facts (rejected by user — recreates the ratification friction MEET-INTEL.1 removed); separate Library destination for boards (rejected by user — sessions are the vision surface); semantic search in V3.1 (deferred — FTS first per minimalism ladder, embeddings arrive with the graph anyway).
**Rationale:** Finishes what the 2026-03 positioning started (meeting intelligence first); structural-first graph makes the living-brain feeling shippable without betting on 14B relationship-extraction quality; immediate-apply learning with auditable memory keeps the zero-friction philosophy while staying inspectable in a privacy-positioned product.

## [2026-07-07] V3.1 planning decisions — smoke folded into acceptance; full session pages; broadcast-refetch; all four v3 plans authored up front
**Context:** /nexus-plan V3.1. The roadmap gated V3.1 on the LIVE.2/3 real-meeting smoke test; user answered "we'll have to test it with v3" — an explicit override folding the smoke into V3.1's acceptance. User also asked to "plan the other versions" in the same session.
**Decision:** (1) The deferred LIVE smoke is now part of V3.1 Task 6's manual acceptance — deterministic rails (chip accept, auto-push) don't depend on it; only chat-driven assistant actions do. (2) Post-hoc sessions become FULL ROUTED PAGES (/session/:id, one SessionWorkspace concept for live + post-hoc); MeetingDetailModal is deleted, its meeting-detail/* sections reused. (3) Live board visibility uses a single main-process `data:changed` broadcast helper called at every mutation site + debounced renderer refetch — no delta-event protocol. (4) Embedded board is fully interactive (boards lose nav, so it's the primary surface); BoardPageModern becomes a thin wrapper around the extracted EmbeddedBoard, its route kept for deep links. (5) Off-canvas assistant actions badge the target tab; never auto-flip. (6) FTS uses query-time to_tsvector('simple') — multilingual transcripts, no migrations. (7) V3.2-V3.4 are authored NOW as parked plans in .planning/plans/PLAN-V3.{2,3,4}.md (PLAN.md holds only the active plan); each parked plan opens with a promotion checklist (revalidate forward file references; V3.4 additionally gates on empirical pgvector-in-PGlite + LM Studio /v1/embeddings preflights with a STOP condition).
**Alternatives:** Refusing to plan until the smoke ran (rejected — user override; most of V3.1 is LLM-independent); keeping the modal one more phase (rejected — V3.2's Brain needs page real estate; bolting review onto the modal is double rework); delta-based board sync (rejected — complexity without measurable benefit at local latencies); one mega-PLAN.md for all of v3 (rejected — breaks the 6-task executor contract).
**Rationale:** Planning momentum with risk explicitly parked beats a hard block the user has already overridden twice; parked plans preserve today's design context (granularity, budgets, stop conditions) without pretending post-V3.1 file paths are facts.

## [2026-07-07] Living-graph vis library: react-force-graph-2d (canvas), with vanilla force-graph wrapper as fallback
**Context:** V3.2 needs a force-directed, incrementally-updatable, themable graph canvas in React 19 + Electron; roadmap required the choice be recorded here.
**Decision:** react-force-graph-2d (MIT, canvas-rendered, supports incremental data mutation for the live-bloom effect and handles thousands of nodes). Graph granularity: nodes = sessions/projects/cards/accepted-suggestions; boards/columns are NOT nodes (column rides as a label on the card→project edge). Perf discipline: node cap with aggregate collapse (~1500), physics paused when tab hidden, prefers-reduced-motion respected.
**Alternatives:** React Flow/@xyflow (rejected — node-editor, not physics graph); raw d3-force custom renderer (rejected — most control, most code; revisit only if the wrapper fights React 19); sigma.js (rejected — WebGL power unneeded at personal scale). Fallback locked: wrap vanilla force-graph in a ~50-line component if the React wrapper has React 19 peer issues — verify at V3.2 promotion.
**Rationale:** Best-fit physics + incremental updates with the least code; the fallback keeps the same underlying engine so styling/physics work transfers.

## [2026-07-07] SUPERSEDED ↑ — the Brain is a dynamic MIND MAP (d3-hierarchy + d3-zoom, pure SVG), not a force-directed graph
**Context:** User provided a reference image for the V3.2 Brain: a horizontal collapsible mind map — root on the left, curved bezier links fanning right, rounded-rect nodes, a circular expand/collapse chevron per branch. This supersedes the react-force-graph-2d decision above before any V3.2 code was written.
**Decision:** BrainMindMap = d3-hierarchy `tree()` layout + d3-zoom pan/zoom in a pure-SVG React component. Hierarchy: workspace → projects → (Sessions group + board columns) → sessions/cards; session scope roots at the session with Project / Cards created / Decisions / Questions branches. Chevron toggles expansion (collapsed shows child count); label click opens the entity; default expansion depth 1. Cross-hierarchy relations (provenance, suggestion→card) render as dashed overlays only on hover/selection — never as permanent edges. Live growth inserts nodes under their parent (animated when the path is expanded, badge on the collapsed ancestor otherwise). No physics → no continuous rAF → the ~1500-node cap and hidden-tab physics-pause mitigations from the superseded decision become unnecessary (collapse IS the scaling mechanism).
**Alternatives:** Keeping force-directed as a second view mode (rejected — two renderers to maintain, and the user's mental model has been "tree" from the first discussion); mind-elixir / markmap libraries (rejected — opinionated shells over what is ~200 lines of well-trodden d3 SVG here, harder to theme with Tailwind tokens); React Flow + dagre layout (rejected — heavyweight for a read-mostly tree; HTML-node convenience doesn't outweigh the dependency). Force layout may be revisited at V3.4 only if semantic entities make the data meaningfully non-hierarchical.
**Rationale:** The data is dominantly hierarchical and the reference interaction is exactly a tidy tree; d3-hierarchy/d3-zoom are tiny, typed, and give full control of the reference look; event-driven SVG rendering has zero idle GPU cost — a hard requirement this app already fought for once.

---

## [2026-07-07] V3.1 execution — two coexisting boards resolved with an `active` guard; ultracode adversarial review folded into the plan
**Context:** /nexus-execute V3.1 (6 tasks) shipped the session-centric rebuild: 3-entry nav, /session/:id pages (MeetingDetailModal deleted), a `data:changed` broadcast + debounced refetch, EmbeddedBoard extracted from BoardPageModern, the Transcript|Board|Brain live canvas, an activity feed, and query-time FTS. A 22-agent adversarial review (6 dimensions × 2-lens verify) over the full diff found 7 real, V3.1-introduced bugs that 766 green tests + tsc + lint all missed.
**Decision:** (1) **Board coexistence model = an `active` guard on `useBoardController`.** Because `LiveModeOverlay` is a `document.body` portal that layers over the still-mounted route, an overlay-owned EmbeddedBoard and a route-owned EmbeddedBoard can be mounted at once, both driving the single global `boardStore` and both registering document-scoped drag monitors → wrong-board display + duplicate drag side-effects. Resolved by making a covered route board inert: `active={!(isRecording && !liveModeMinimized)}` on route boards (BoardPageModern + SessionWorkspace Board tab); the overlay board stays `active`. An inert controller registers no monitors and does not `loadBoard` (no stomp), and self-heals (reloads its projectId) when `active` flips back true. The overlay-visible predicate `isRecording && !liveModeMinimized` is the canonical "is the live takeover covering the route" signal — reuse it for any future surface that coexists with the overlay. (2) **`useBoardLiveSync` refetch re-reads the current board at timer-fire** (not the id captured at event time) so navigation within the 300ms debounce never resurrects the old board. (3) **The agent `moveCard` tool now broadcasts** — every meeting-agent tool write path hits `notifyDataChanged`. (4) **Per-card Reject/Keep for auto-pushed cards lives on the session Board tab** (real KanbanCardModern cards), NOT reimplemented on the page — there is no "list a meeting's cards" IPC and building one to fake it was rejected as fabrication.
**Alternatives:** Per-project keying of `boardStore` (rejected — a larger, riskier re-architecture than the contained `active` guard; the guard fully addresses both the stomp and the duplicate-monitor findings); scoping drag monitors by `canMonitor` board-id (rejected — the `active` guard is simpler and also fixes the load-stomp, which a canMonitor scope would not); deferring the board-concurrency findings to ISSUES.md (rejected under ultracode — they degrade the headline "watch the assistant work" flow, and the fix proved low-risk with a regression suite).
**Rationale:** The overlay-over-route topology is intrinsic to the full-screen live takeover, so the coexistence had to be modelled explicitly rather than assumed away; the `active` guard is invisible to users (an inert board is always fully covered) yet deterministically prevents the store stomp and duplicate side-effects. Running the adversarial review before the user's smoke test caught defects (infinite-spinner dead-end, wrong-board race, onboarding tour walking new users through deleted sections) that no automated gate surfaced — validating the ultracode "verify before you trust green" posture on a large, MEDIUM-confidence diff.

## [2026-07-07] Brain live growth: refetch-and-diff, not event→insert (V3.2 promotion revalidation)
**Context:** Promoting PLAN-V3.2 into PLAN.md. The parked Task 4 mapped renderer events to targeted mind-map inserts ("new card → insert under its column node"), with dedupe + a >25-delta refetch fallback. Revalidation against the shipped V3.1 code showed the premise is false: the `data:changed` payload (dataChangeNotifier.ts) is `{ scope: 'cards'|'columns'|'projects', projectId? }` — scope-level by design, no entity identity — so the renderer cannot know WHAT changed, only that something did.
**Decision:** brainStore refetches the active scope's full tree (300ms trailing debounce on `data:changed`, mirroring useBoardLiveSync) and DIFFS node-id sets (ids stable, derived from entity type + id) to find entering nodes — those drive entrance animations, collapsed-ancestor chevron badges, and the Brain canvas badge. Decision/question chip accepts, which never emit `data:changed` (no card mutation), trigger the same refresh through the liveSuggestionsStore accept() success seam — the exact hook that already feeds activityFeedStore. The parked dedupe/threshold machinery is deleted.
**Alternatives:** Enriching `data:changed` payloads with entity ids + parent paths so targeted inserts work (rejected — adds main-process surface and a second consumer contract to keep honest at every mutation site, for marginal gain when the full tree is hundreds-to-low-thousands of nodes over one IPC); keeping event→insert only for the suggestion-accept path where rich data exists (rejected — two growth code paths that must agree beats nothing; one path that cannot drift wins).
**Rationale:** Refetch-and-diff is one code path for every trigger, idempotent under the broadcaster's known double-fires (they collapse into a single debounced refetch; an unchanged id-set animates nothing), and strictly simpler than what it replaces — the scope-level broadcast was V3.1's deliberate "impossible to forget at new mutation sites" design, and this keeps V3.2 a pure consumer of it.

## [2026-07-08] V3.3 execution — twin interview extraction is a plain invoke (not streaming); profile block prepends to the system prompt; wizard review writes all sections
**Context:** /nexus-execute V3.3 (5 tasks, 916→989 tests, zero retries). Three implementation forks surfaced during execution that the plan left open or worded ambiguously.
**Decision:** (1) **The per-step "Interview me" extraction is a non-streaming `ipcMain.handle('twin:draft-section')` → `twinInterviewService.draftSection`**, mirroring liveTriageService's validate-retry-skip discipline (per-section zod schemas; no model → skip; malformed after one retry → skip; never rejects for AI reasons) — the plan's `<files>` said "reusing streaming agent infra," but the locked contract was the JSON validate-retry-skip discipline, and a one-shot request→JSON extraction gains nothing from the meeting-agent streaming channel (fullStream loop, per-meeting abort map, event plumbing). (2) **The twin profile block is PREPENDED to each consumer's SYSTEM prompt** (`${profileBlock}\n\n${baseSystemPrompt}`, header `User profile (the professional you assist):`), matching the existing preamble convention in meetingIntelligenceService; injection is defensive (a twinProfileService failure can never break a prompt build) and read-at-call-time (profile edits apply on the next prompt); with no profile every prompt is byte-identical to pre-V3.3, proven by per-consumer regression tests. The live-assistant injection lives in `ipc/meeting-agent.ts` because that is where the system prompt is actually assembled — the plan's file list named only meetingAgentService.ts. (3) **The wizard's review step writes all 7 sections** (empties prune to `{}`/`[]`), not just dirty ones — full-editor "refine" semantics where clearing a section in the wizard persists the clear; the service API stays `updateProfileSection(section, value)` only (no bulk patch — YAGNI, the form saves per section).
**Alternatives:** Wiring the meeting-agent streaming channel for the interview (rejected — real complexity, zero UX benefit for a one-shot extraction; revisit only if the interview ever becomes conversational); appending the profile to the user/transcript prompt (rejected — the story's budget rule "shrink the profile, never the transcript" requires the transcript prompt untouched); dirty-section-only writes on wizard finish (rejected — makes "I cleared my goals" silently non-persistent).
**Rationale:** Each fork was resolved toward the smallest implementation that preserves the locked contracts (validate-retry-skip, byte-identical no-profile behavior, form-owns-data), with the deviation from the plan's literal wording documented in code and here rather than silently absorbed.

---
## [2026-07-08] V3.3.5 execution — parallel-batch contract-freeze; merge-not-replace refine seed (data-loss fix); provider-native web search (supported); Gemini as frontier provider
**Context:** /nexus-execute V3.3.5 (6 tasks, 989→1126 tests). Executed as a parallel batch and hardened by a 23-agent adversarial review. Several forks and one serious defect were resolved during execution.
**Decision:**
(1) **Deep-creation drafts MERGE over the existing profile on refine; they never replace it.** `TwinWizard.seedFromPanelDraft` seeds the shared editable review from a deep/history/web `Partial<TwinProfileSections>` draft via `seedSuppliedSections(prev, draft)`, which overrides ONLY the sections the draft actually supplies (`'key' in draft`) and preserves every section it omits. This closes a HIGH-severity data-loss bug the adversarial review found (3 dimensions, both verify lenses): because `twinProfileService.updateProfileSection` is a per-section jsonb REPLACE and the wizard's `writeAllSections` writes all 8 sections on save, the previous "reseed drafts from the panel draft alone" wiped stored `identity`/`preferences` (which history mining structurally excludes) with `{}`. Guarded by `TwinWizard.refineMerge.test.tsx` (failing-before/passing-after). `brief` remains special-cased.
(2) **The deep interview lives in a NEW `twinDeepInterviewService` (interviewNext/interviewSynthesize), not `twinInterviewService`** — keeps the V3.3 `draftSection` seam untouched and gives the frozen IPC a stable delegate. Transport is invoke-per-turn (extends the V3.3 invoke-not-streaming decision): each turn is a small request→JSON via `taskType:'twin_interview'`, validate-retry-skip, hard 8-question cap.
(3) **Web research uses provider-native server-side search ONLY, and the installed stack SUPPORTS it** — the Task-4 preflight (reading node_modules types, not memory) confirmed `ai@6` + `@ai-sdk/{anthropic,openai,google}` expose `webSearch_20250305`/`webSearch`/`googleSearch` with citations on `steps[].sources`. Capability is gated on `twin:get-creation-model.isFrontier`; local/no-model providers render an honest absence. No scraping, no new search-API key surface (the STOP-condition was not triggered).
(4) **Google Gemini is a first-class frontier provider**, wired exactly like OpenAI/Anthropic. `@ai-sdk/google` is pinned to `^3.0.90` (provider-spec-v3, compatible with `ai@6`) — NOT 4.x, which pulls provider-v4 and mismatches the rest of the SDK. `'google'` was added to `AIProviderName` and every exhaustive `Record<AIProviderName,…>` map + the IPC validation schema; `FRONTIER_PROVIDERS = openai|anthropic|google` is the single definition of "frontier".
(5) **Cloud-routed history mining requires a per-run consent dialog (no remember-me); the consent descriptor is computed with ZERO model calls; and only what the descriptor discloses is sent** — the "People who spoke" speaker block was removed from the miner context (it carried only generic "Speaker N" diarizer labels, never real names) so disclosed==sent, closing an adversarial-review consent-copy gap.
(6) **Execution methodology: contract-freeze parallel batch.** Task 1 created/froze every shared file (schema, `src/shared/types/twin.ts`, 6 IPC channels, `ai.ts` frontier types, and honest stub files/components with their mount points), after which Tasks 2/3/4/5 ran as a file-disjoint concurrent batch editing only files they own, with a STOP-and-report seam (no sibling edits to shared files) and tree-wide gates run only post-batch. A follow-up round solidified the draft→review seam BEFORE the batch spawned, preventing a mid-batch double-STOP.
**Alternatives:** Append/dedupe list sections on refine instead of override-supplied (rejected for now — override-supplied is the minimal correct data-loss fix; the user reviews before save, and list-merge semantics can be a later refinement); polyfilling web search via scraping/a new search key if unsupported (pre-authorized STOP path — not needed since native search is present); adding `'google'` to `AIProviderName` in Task 1 (rejected — it would have broken Task 1's own exhaustive-map tsc gate; deferred to Task 5 which owns those maps); one big sequential execution (rejected — the batch is file-disjoint by construction, so concurrency is safe and much faster); counting speakers in the consent dialog instead of removing the block (rejected — the block is useless generic labels, so removal both closes the disclosure gap and drops dead prompt tokens).
**Rationale:** The refine merge preserves the project's non-negotiable "never delete local data" sync-safety principle at the one place V3.3.5 could have violated it; the batch methodology got four features landed collision-free by making Task 1's frozen contracts the only cross-agent coupling; and the adversarial review (which found the data-loss bug that 1118 green tests, tsc, and eslint all missed) is the same "verify before you trust green" discipline that paid off on V3.1 — here it also pruned an overstated privacy finding to its true minor form.

## [2026-07-09] V3.4 planning — RAG ships a cited "Ask" answer; embeddings default multilingual with dimension-by-measurement
**Context:** /nexus-plan v3.4 promoted the parked PLAN-V3.4 (twin learning loop + semantic layer, final v3 phase). Revalidation against `90cc7bb` confirmed the pgvector artifact is bundled in @electric-sql/pglite@0.3.15 and drizzle-orm 0.45.1 has a native `vector()` type, and corrected two stale file references (graphService→brainTreeService, BrainCanvas→BrainMindMap). Two scope forks needed user decisions.
**Decision:** (1) **V3.4 search ships retrieval PLUS answer synthesis:** hybrid FTS+vector results, and an explicit "Ask" action (never per-keystroke) that feeds top-K retrieved chunks to one `knowledge_qa` model call producing a short grounded answer with source-session citations; no-model/failure degrades to plain results, and thin retrieval must yield an honest "I don't find that in your sessions". (2) **The embedding default is MULTILINGUAL:** the Settings hint recommends an EmbeddingGemma-300M-class model (768-dim; bge-m3 fallback; nomic-embed-text-v1.5 noted for English-only), with LM Studio availability verified — and the schema dimension locked by live MEASUREMENT — at the Task 1 preflight; the index records the embedding model id, and a model mismatch surfaces a "rebuild index" affordance rather than silently mixing vector spaces. (3) Fact/entity extraction adds no per-run consent dialog: it consumes only the current session's brief + accepted suggestions (data the brief task already routed through the user's configured model), so the per-task Settings choice remains the consent surface; the per-run dialog stays reserved for history-WIDE mining, and bulk embeddings carry an explicit no-silent-cloud guard.
**Alternatives:** Retrieval-only hybrid search deferring answers (rejected — PROJECT.md's success metric is "answered from past sessions in one search"; grounded QA is within a 14B local model); English-only nomic default (rejected — meetings are German/mixed and multilingual transcription already shipped; English-only embeddings retrieve German content poorly); hardcoding 768 dims from research notes (rejected — measurement at preflight tolerates any model swap).
**Rationale:** The Ask card is the literal fulfillment of the v3 promise at the cost of one extra model surface with a clean degradation path; dimension-by-measurement plus recorded model provenance makes the embedding-model recommendation swappable without schema risk; keeping the consent boundary at "session-scoped = task routing, history-wide = per-run dialog" preserves the V3.3.5 consent model without adding friction to an automatic background loop.

---
## [2026-07-10] V3.4 execution — LM Studio echoes the real embedding model (provenance from response.model, not the requested id); forgotten facts gate re-learning; card-dominant Ask answers suppress session citations
**Context:** /nexus-execute V3.4 (the FINAL v3 phase — twin learning loop + local semantic layer; 6 tasks, 1154→1279 tests). Executed as preflight+contract-freeze (Task 1) + a `search:ask` seam follow-up → two file-disjoint parallel batches (2+4, then 3+5) → a 28-agent adversarial review (8 dims × 2-lens, 10 raw findings → 6 survived) → a dedicated fix pass → Task 6 integration. Several execution-time discoveries and forks were resolved.
**Decision:**
(1) **Embedding-model provenance is the provider-ECHOED `response.model`, never the requested id.** The user-assisted preflight discovered LM Studio's `/v1/embeddings` silently routes to whatever embedding model is loaded and ignores a requested id that isn't a loaded embedding model (proven: requesting the chat model `gemma-4-12b-qat` returned nomic-v1.5's vectors with `response.model = nomic`). So `embed()` surfaces the echoed model, `embedding_index_meta` records it, and BOTH the write-side and read-side mismatch guards compare the echoed model — otherwise the "index built with X, now Y → rebuild" affordance is blind. `EMBEDDING_DIM=768` was locked by live MEASUREMENT of EmbeddingGemma-300M (multilingual), not from research notes.
(2) **`resolveTaskModel('embedding')` returns null when unconfigured (no silent cloud fallback for BULK content), and the privacy hint is provider-aware at the point of choice.** An adversarial finding caught the embedding Settings hint asserting "stays on your device" even when a cloud provider was selected — fixed to mirror the `live_assistant` provider-gated hint (amber warning for cloud). No-silent-cloud made honest at the decision surface.
(3) **Forget is DURABLE: fact-extraction dedupe filters candidates against ACTIVE *and* FORGOTTEN facts**, but forgotten content is never re-disclosed to the model (post-generation filter, not a prompt exclusion). An adversarial finding showed dedupe-vs-active-only silently resurrected a forgotten fact when a later session re-mentioned it — defeating the safety-triad Forget control.
(4) **Read-side embedding-model mismatch records the rebuild flag.** `retrieveVectorHits` returning `[]` on mismatch (correct — never mix vector spaces) previously surfaced no affordance on a fully-indexed static corpus; it now persists the same `embedding.indexMismatch` setting the write path uses, via a helper exported from embeddingService.
(5) **Ask citations = cite-what-was-retrieved (a deliberate RAG convention), with one guard:** when the single closest grounding chunk is a card, session citations are suppressed (a card-dominant answer must not attribute to co-retrieved sessions). The fragile alternative (LLM emitting `[n]` usage indices) was rejected. The no-answer sentinel match was hardened to fold typographic apostrophes.
(6) **Card embeddings store `meetingId=null`** (not `sourceMeetingId`) so the `embeddings.meetingId` ON DELETE cascade cannot drop a surviving card's embedding when its origin meeting is deleted.
(7) **Post-session ordering (facts → entities) is guaranteed by ES-module import evaluation**, not boot-file ordering: `entityService` imports `isLearningPaused` from `twinMemoryService`, so twinMemoryService's top-level hook self-registration runs first. Entity extraction rides `twin_learning` routing + the pause gate and is capped flat (≤8 person/topic entities/session, dedupe by normalizedName); NO entity↔entity relationships in v3 (the 14B relationship-extraction cliff — a hard ceiling).
**Alternatives:** Trusting the requested embedding-model id for provenance (rejected — LM Studio proves it lies); a hard schema dimension from research (rejected — measured 768 tolerates model swaps); dedupe forgotten facts by re-adding them to the prompt exclusion list (rejected — would re-disclose forgotten content to the model; a post-generation filter keeps them out of sight); LLM `[n]`-index citations (rejected — model-dependent/fabrication-prone, per the refuting verifier); folding the review fixes into Task 6 (rejected — a dedicated fix pass kept each finding focused with a fail-before/pass-after test before integration).
**Rationale:** The phase's two hard assumptions (pgvector-in-PGlite, live local embeddings) were converted to evidence before any schema commit, and the preflight itself surfaced the LM Studio routing behavior that reshaped provenance. Contract-freeze made two 2-agent concurrent batches collision-free; the one missing seam (`search:ask`) was patched via a Task-1 follow-up, never a sibling edit. The 28-agent adversarial review caught 6 real defects (3 MED privacy/consent/correctness) that 1252 green tests missed while its 2-lens verification refuted 4 non-issues (incl. a plausible queue lost-wakeup proven unreachable in the single-threaded event loop) — the same "verify before you trust green" discipline that paid off on V3.1 and V3.3.5, now closing out the v3 program.

---
## [2026-07-21] GUARD.1 plan — inactivity auto-stop signal/placement, OFFICIAL_BUILD enforcement over hardcoding, main-process local-only transcription guard
**Context:** /nexus-plan after the v3 program completed. User-requested scope: a "forgot LifeDash was recording" protection (detect call end / no activity, notify, auto-stop), plus two deferred items (OFFICIAL_BUILD fix, Settings privacy polish). Phase G (calendar) was explicitly split into its own follow-up plan; INTEL-FIX.1 and ISSUES.md I2/I3 stay deferred.
**Decision:**
(1) **Auto-stop behavior = notify-then-stop:** after N min of sustained silence (default 10, configurable 2..120, feature ON by default) show an in-app countdown banner + desktop notification; one-tap "Keep recording"; a fixed 2-min countdown expiry calls the normal clean `stopRecording()` path. Brief generation stays deferred to session-open (existing pipeline behavior, no new trigger).
(2) **Detection signal = audio RMS silence (renderer-side), not transcript gaps.** Music/screen-share audio keeps a session alive while a dead call goes silent; transcript-gap detection would false-positive on speech-less activity. Detector polls the pull-based `getAudioLevel()` every 5s and reuses AudioLevelMeter's SILENCE_EPSILON=0.01 — the push slot `onAudioLevel` is single-subscriber and owned by the meter, so the detector must never claim it. Renderer placement because capture itself is renderer-side (electron-audio-loopback) and main has no level signal; renderer stays alive in tray-mode recording.
(3) **OFFICIAL_BUILD: enforce, don't hardcode.** Investigation showed the deferred item was HALF-fixed by `5a6593a` (build-time define + gated updater) but nothing sets the flag: preflight-release.js doesn't check it and the macOS CI workflow doesn't set it — so every macOS release to date shipped with auto-update compiled out. Fix = preflight fail-fast on `OFFICIAL_BUILD !== 'true'` + `OFFICIAL_BUILD: 'true'` in release.yml's make env. Hardcoding `true` in vite.main.config.ts was rejected — it would un-gate fork/dev builds, undoing `5a6593a`'s fork-safety intent.
(4) **AssemblyAI opt-out enforcement = a `transcription:localOnly` setting enforced in MAIN** (cloud-dispatch guard + setProvider rejection), not renderer-only UI state — a privacy control that UI state can bypass is not a control. Plus a consent dialog on local→cloud provider switches (per-switch, TwinResearchConsentDialog pattern) and Local/Cloud privacy pills + a Kimi warning line (descriptive copy, not ranking-based). Default localOnly=false so existing cloud users are not broken.
**Alternatives:** Transcript-segment-gap detection in main (rejected — false-positives on non-speech audio; main lacks the level signal anyway); silent auto-stop or notify-only (user chose notify-then-stop); cross-env forcing OFFICIAL_BUILD inside the npm publish script (rejected — preflight fail-fast keeps "official" an explicit human act while still blocking the forgotten-env failure); one mega-plan including Phase G (rejected — 8 calendar deliverables can't share a 6-task plan; user approved detection-first sequencing).
**Rationale:** The auto-stop design rides four verified existing seams (getAudioLevel pull, stopRecording clean path, ViewingProjectBanner mount, notificationShow IPC) — no new infrastructure. The OFFICIAL_BUILD finding upgraded the deferred item from "hardcode a flag" to a real release-integrity fix with a concrete blast radius (all macOS releases). The privacy work converts prose-only trust signals into an enforced boundary plus visible badges, mirroring consent patterns the codebase already established in V3.3.5.

---
## [2026-07-21] GUARD.1 execution — local-only enforcement expanded mid-task to a third, previously-unknown cloud-dispatch site
**Context:** /nexus-execute GUARD.1 (6 tasks, 1282→1342 tests). Executed as Task 1 sequential (froze the inactivity-detector contract) → Tasks 2/3/4/5 concurrent batch → Task 6 integration. Task 4's own verify-step grep (checking "no other call site constructs a cloud transcriber unguarded") surfaced a target the plan did not name: `src/main/services/speakerDiarizationService.ts`'s `diarizeMeeting()`, whose `resolveProvider()` picks a cloud provider independent of the active-provider setting — so a user with a stale/leftover Deepgram or AssemblyAI API key configured would still have their full meeting WAV sent to the cloud for diarization even with `transcription:localOnly` on and the active provider set to local.
**Decision:** Authorize the fix as part of Task 4, in scope, rather than deferring it to a follow-up. `diarizeMeeting()` now blocks outright under local-only (`{success:false, error:'Local-only transcription is on — speaker diarization requires a cloud provider and is unavailable.'}`) — no local fallback, since Whisper cannot diarize. Also recorded a Windows-specific process-incident lesson from Task 6 (see STATE.md's "Process Note" and the new LEARNINGS.md): a temporary `git worktree` NTFS-junctioned to `node_modules` for baseline eslint diffing had its `node_modules` wiped when the worktree was removed (Windows recursive-delete follows junctions) — recovered via `npm ci`, no tracked files affected, independently re-verified by the orchestrator before accepting the phase.
**Alternatives:** Ship Task 4 as originally scoped and open a follow-up issue for `speakerDiarizationService.ts` (rejected — the plan's own SPEC.md requirement for this phase states local-only MUST block ALL network transcription; shipping with a known, verified, live bypass would make that requirement false on day one, and the fix was a small, contained, no-local-fallback block rather than a redesign). Silently expanding Task 4's file list without flagging it (rejected — the task-worker correctly stopped and reported per its Stuck Detection rule for "a third cloud-dispatch site... outside my Files-to-Modify list," and the orchestrator explicitly authorized the fix rather than letting scope creep in unflagged).
**Rationale:** A privacy-enforcement task that ships with a known, live bypass in the same commit is worse than a task that takes one extra authorized step to close it — the whole point of GUARD.1's local-only work was "a privacy control that UI state can bypass is not a control," and an unguarded third main-process dispatch site is the same failure mode one layer down. Escalate-and-authorize (rather than either silently expanding scope or silently skipping the finding) kept the fix auditable: a single sub-agent message documents exactly what was found, why it was in-scope, and what was decided.

---
## [2026-07-23] Phase G — BYO OAuth client IDs (user-registered Google/Azure apps), not embedded developer credentials
**Context:** /nexus-plan Phase G (calendar). Calendar OAuth needs client credentials from somewhere: embed the developer's app registrations in the build, require each user to bring their own, or hybrid. Google's calendar scope is "sensitive" — a publicly distributed embedded client triggers the unverified-app warning, a lifetime 100-user cap, and eventually Google's app-verification process.
**Decision:** BYO client IDs (user choice): the user registers their own free Google Cloud Desktop-app OAuth client (ID + secret) and Azure public-client app (ID only), pastes them into a guided Settings section, and the guided copy includes the critical "set the Google consent screen to In production" step (Testing status expires refresh tokens every 7 days). Settings stores client config encrypted via safeStorage, same as tokens.
**Alternatives:** Embedded developer IDs (rejected — verification burden + 100-user cap on a sensitive scope for a distributed AGPL app, and IDs would sit in the public repo); hybrid build-time env override (rejected for now — most code paths to test with no current need; trivially addable later since client config is already data, not code).
**Rationale:** Matches the product's BYOK positioning exactly (users already bring AI API keys); zero verification/quota coupling between users; nothing secret ships in the repo. The one-time setup friction is accepted and mitigated by verbatim in-app step lists.

## [2026-07-23] Phase G — hand-rolled AC+PKCE via system browser + 127.0.0.1 loopback; no MSAL/googleapis dependencies
**Context:** No OAuth infrastructure exists in the codebase (recon-proven: no http.createServer anywhere; the Supabase auth window is a form-in-BrowserWindow pattern that cannot receive OAuth redirects, and Google has hard-blocked OAuth in embedded webviews/Electron windows since 2023 — disallowed_useragent).
**Decision:** One cohesive `calendarAuthService` implements authorization-code + PKCE (S256) for both providers: `shell.openExternal` to the system browser, one-shot `node:http` listener on `127.0.0.1` ephemeral port (Microsoft additionally best-effort dual-binds `::1` on the same port because its registered `http://localhost` redirect may resolve to IPv6), `state` validation, 5-min timeout. Asymmetric exchange encoded explicitly: Google Desktop clients send their (non-confidential) client_secret; Microsoft public clients must send none. Scopes: Google `openid email calendar.events.readonly`; Microsoft `openid email offline_access Calendars.ReadBasic` (fallback `Calendars.Read` sanctioned if ReadBasic omits attendees in practice). Tokens encrypted via the existing safeStorage→settings-KV pattern (`storeRefreshToken` analog). Task 1 ends in a LIVE both-provider auth round-trip with a STOP condition.
**Alternatives:** `@azure/msal-node` + `google-auth-library` (rejected — two heavyweight deps to wrap a protocol that is ~200 lines against stable documented endpoints, in a codebase that already hand-rolls Supabase REST auth; revisit only if the live preflight exposes protocol complexity the plan missed); embedded BrowserWindow auth (rejected — Google hard-blocks it); custom protocol handler redirect (rejected — Google doesn't support it for Desktop clients; loopback is the documented pattern).
**Rationale:** Minimalism ladder (stdlib over new deps) with the anti-fabrication guardrail satisfied by research-verified endpoints/parameters (sourced in PLAN.md) plus a live preflight that converts the remaining doc ambiguity (Google's soft "Optional" client_secret wording) into evidence before dependent tasks run.

## [2026-07-23] Phase G — calendar data policy: metadata incl. attendees, never bodies; email-matching replaced by series-association learning
**Context:** The roadmap's Phase G text contradicts itself — "free/busy data only" alongside deliverables requiring event titles and attendee hints — and its attendee→project matching assumed project-side email metadata that recon proved does not exist anywhere in the schema (only person names in `entities`/`twinProfile.people`; the sole stored email is the user's own sync login).
**Decision:** (1) Storage policy resolves the contradiction (user choice): fetch event metadata, persist ONLY title/start/end/attendee names+emails/series-id into local PGlite; event bodies/descriptions/locations are never persisted — enforced structurally (the `CalendarEvent` type has no body field) and contractually (SPEC.md MUST NOT). (2) Attendee-email matching is replaced by deterministic series-association learning: a recurring series recorded ≥2 times against the same project auto-suggests that project (derived from new `meetings.calendarSeriesId`, so it survives provider disconnect); attendee NAMES (never emails) additionally ride the existing project-detection prompt as an optional labeled hint through the same task-routed model that already sees the full transcript. (3) No auto-record on any calendar signal — explicit click only (roadmap privacy floor, reaffirmed).
**Alternatives:** Literal free/busy-only (rejected — kills the phase's headline "Start recording for {title}" affordance); building a new attendee/email↔project store for email matching (rejected — new PII surface + fuzzy matching for marginal gain over series learning; revisit only if multi-provider series ids prove unstable); fuzzy name-matching attendees against `entities` for auto-suggest (rejected — non-deterministic auto-behavior in a trust-sensitive spot; names serve better as soft LLM context).
**Rationale:** The policy keeps the privacy-first promise honest at the correct layer (what is *stored and injected*, not which scope string is requested — no provider offers titles-without-descriptions scopes); series learning is explainable ("you recorded this series against X twice"), cheap, and needs zero new AI; and the emails-stay-in-DB / names-only-in-prompts line keeps calendar data's AI exposure strictly narrower than the transcript's existing exposure.

---

## [2026-07-24] Phase G — REVERSAL: embedded (build-injected) OAuth client credentials become the default, BYO demoted to optional override
**Context:** During Task 1 execution, before the live preflight, the user reconsidered the 2026-07-23 BYO-client-ID decision on UX grounds: per-user Google Cloud / Azure app registration is genuinely tedious and a known adoption killer versus a standard one-click "Connect with Google/Microsoft" button. Key reframe that unblocked the choice: the OAuth *code* is identical in both models — BYO vs. embedded changes only (a) where the client ID/secret come from (user-pasted vs. baked in at build time) and (b) whether the developer completes provider verification. The developer must register the apps in EITHER model; embedded only spares END USERS from doing so. So no preflight or Task-1 work is wasted by switching.
**Decision:** Embedded is now the default: the developer's Google Desktop client (ID + secret) and Azure public client (ID) are injected at build time via env-sourced Vite defines, mirroring the existing `OFFICIAL_BUILD` build-define pattern (`vite.main.config.ts`). `loadCalendarClientConfig(provider)` resolves as: user-pasted override (if present) → build-time embedded default → undefined. The Task-1 BYO paste path + `calendar:set-client-config` IPC are RETAINED but demoted to a collapsed "Advanced — use your own credentials" section in Settings (Task 6), primarily for AGPL forks/self-hosters who cannot ship the maintainer's credentials and as a fallback while verification is pending. Google app **verification** (sensitive scope: privacy-policy + homepage + brand review; NO CASA security assessment for `calendar.events.readonly`) becomes a PRE-PUBLIC-RELEASE checklist item — unverified embedded is fine for the maintainer's own use + all testing (≤100 users, one warning screen). Microsoft publisher verification is the lighter analog.
**Alternatives:** Keep BYO as default (rejected by user — adoption friction); delete BYO entirely (rejected — the code is already built, near-zero cost to keep, and AGPL forks genuinely need a self-credential path; kept as non-default override, user may still veto); a hosted OAuth broker to hide credentials (rejected — LifeDash is local-first with no backend, and a broker would route user calendar access through maintainer infrastructure, violating the identity).
**Rationale:** The dominant reframe — identical code, developer-registers-either-way — means switching costs almost nothing now while delivering the one-click UX the product needs for adoption. Making verification a release-gate rather than a dev-gate keeps engineering unblocked. Retaining BYO as an override preserves the local-first/AGPL escape hatch and the already-completed Task-1 plumbing. Data storage is unaffected: tokens + event cache remain encrypted on each user's own device in every model.

---

## [2026-07-31] BRAIN-UX.1 — entity knowledge via extraction-time persisted facts (approach A), not on-open RAG
**Context:** The user reported that clicking a person in the Brain opens an effectively empty modal. Recon confirmed this is a data gap, not a display bug: the `entities` table stores only name + kind, and `EntityInspector` renders everything that exists. The user wants the local LLM to connect facts to a person from the transcripts they appear in.
**Decision:** Persist LLM-extracted per-entity facts at extraction time into a new `entity_facts` table (migration 0044) with hard per-fact provenance (`sourceMeetingId` FK, cascade) and one-tap forget — the same auditable-memory triad the twin ledger uses. New meetings extract automatically via a postSessionDispatcher hook; historical meetings are mined ONLY on demand via a per-entity "Analyze past sessions" action (sequential mining, dedupe by (entityId, sourceMeetingId)). Facts cover BOTH entity kinds (persons and topics — user choice).
**Alternatives:** On-demand RAG synthesis when the modal opens (rejected — slow modal on every open at local-LLM latency, repeated compute, results not auditable/forgettable); automatic full history backfill (rejected by user — unbounded local-LLM churn on a large history with no user control); persons-only facts (rejected by user — one extraction pass covers both kinds for marginal extra cost).
**Rationale:** Persisted facts make the modal instant and the knowledge auditable — provenance + forget are PROJECT.md constraints ("twin memory must be auditable"), and consistency with the existing twin-fact pattern beats inventing a second knowledge model. On-demand backfill keeps the local model under user control.

## [2026-07-31] BRAIN-UX.1 — post-meeting chat is Q&A-only and rides the existing meeting-agent thread
**Context:** `LiveAssistantSection` was explicitly deferred as read-only ("Phase A is live-only"). The user copies transcripts into external AI tools to discuss finished meetings — a privacy leak the product exists to prevent — and asked for an in-app chat with a local model about a finished meeting.
**Decision:** Reuse the existing `meeting-agent:send` channel, single per-meeting thread, and streaming events; the service selects a toolset by meeting status — completed meetings get ONLY read-only tools (recent-transcript window, transcript search, meeting context/brief; all already budget-capped for 14B locals) and a "meeting is over, ground answers in the transcript, cite timestamps" system-prompt variant. No side-effect tools post-meeting (user choice). The live-recording path stays byte-identical (test-proven). The renderer keeps the live-phase conversation visible so live and post-meeting Q&A form one continuum.
**Alternatives:** Full tool access post-meeting (rejected by user — action-taking post-hoc needs more careful UX; Q&A matches the copy-to-ChatGPT habit being replaced); a separate ask-style chat on the V3.4 cited-Ask machinery (rejected — cross-session retrieval answers a different question than "discuss THIS meeting", loses the thread continuum, and adds a second chat surface); a new IPC channel/mode flag (rejected — meeting status already encodes the mode; fewer contracts to freeze).
**Rationale:** Maximum reuse of proven machinery (thread, streaming, transcript tools) makes the highest-value feature of the phase a low-risk change, and Q&A-only keeps the trust model simple: after the meeting, the assistant informs but never acts.

## [2026-07-31] Single v2.5.0 release instead of the planned 2.4.0 → 2.5.0 double ceremony
**Context:** The recorded plan was two sequential releases ("2.4.0 then 2.5.0 in order"), written when Phase G was expected to ship before BRAIN-UX.1 executed. Both phases (plus INTEL-FIX.1 and the Intel nav restore) are now on main at the same HEAD — a v2.4.0 built from HEAD would contain BRAIN-UX.1/INTEL-FIX.1 code under a label that doesn't claim it, the same provenance-mismatch shape as the v2.3.0 incident.
**Decision:** Skip the 2.4.0 tag entirely; ship one v2.5.0 from HEAD. The 2.4.0 releaseNotes draft was merged into the 2.5.0 entry (calendar items first), plus a Fixes section for INTEL-FIX.1 and an item for the Intel nav restore. Release history keeps 4 entries (2.5.0, 2.3.1, 2.2.40, 2.2.39).
**Alternatives:** Two ceremonies from the same HEAD (rejected — 2.4.0 binaries would silently contain 2.5.0 features); tagging 2.4.0 retroactively at the Phase G tip `f49c34b` (rejected — version bump requires a commit, so the tag couldn't point at a clean historical tree, and nobody ever ran a packaged smoke on that exact tree).
**Rationale:** Version labels must match binary contents — the v2.3.0 provenance mismatch is the standing lesson. Nothing is lost: the in-app changelog documents the calendar work under 2.5.0.

## [2026-07-31] macOS CI gets the embedded calendar OAuth creds via repo secrets
**Context:** Embedded calendar credentials are build-injected from env by `vite.main.config.ts` defines; the local Windows publish reads the gitignored `.env`, but `release.yml`'s make step set only `OFFICIAL_BUILD`/`SKIP_OBFUSCATION` — so every macOS CI build would embed empty strings and macOS users would silently get no built-in calendar client (BYO Advanced path only; code degrades gracefully, test-proven).
**Decision:** Pass `GOOGLE_CALENDAR_CLIENT_ID`/`GOOGLE_CALENDAR_CLIENT_SECRET`/`MICROSOFT_CALENDAR_CLIENT_ID` as GitHub Actions secrets in the macOS build step. Accepted knowingly: this embeds the Google client secret in the public macOS binary — the identical tradeoff already ratified for Windows builds (Google's desktop-OAuth model treats the "secret" as non-confidential; PKCE + loopback carry the actual security).
**Alternatives:** macOS = BYO-only (rejected — silently platform-inconsistent out-of-box experience, contradicts the embedded-first Settings UX shipped in Phase G Task 6); committing the creds to the repo (rejected — repo-public plaintext is strictly worse than build-time injection).
**Rationale:** Platform parity for the headline 2.5.0 feature; the secret-in-binary tradeoff was already decided for Windows on 2026-07-31 (BYO→EMBEDDED reversal) and CI secrets merely extend it to the other official build channel.

## [2026-07-31] CAL-UX.1 folded into the unpublished v2.5.0 draft — always-visible agenda, 7-day horizon, calendar picker
**Context:** During the v2.5.0 smoke the user saw no calendar presence at all and invoked the original intent: "the calendar should be visible at all times and connected with whatever the user selects." Investigation: the ribbon is a 15-min window by design; the "always-visible" list hides itself when the 24h window is empty (Friday evening ⇒ nothing renders); the poller fetches only 24h ahead; Google reads only the primary calendar; there is no calendar-selection UI. The draft was untagged/unpublished, so rework was cheap.
**Decision:** User chose ALL THREE fixes in 2.5.0 (hold the release): (a) persistent agenda section on Sessions home with an empty state — never vanishes while a calendar is connected; (b) 7-day horizon (single shared CALENDAR_LOOKAHEAD_HOURS=168, also unifying the pre-existing 24-vs-48 constant duplication); (c) per-provider calendar picker in Settings. Placement: Sessions home agenda (user choice over sidebar-everywhere or a full calendar view).
**Alternatives:** Ship 2.5.0 as-is and fix in 2.5.1 (rejected by user); visibility fixes only with picker as follow-up (offered as recommendation, declined — user wants the full intent in one release).
**Rationale:** The draft was not public, so the only cost is a rebuild + re-smoke; shipping a UX the user had already flagged as not matching the agreed intent would have made 2.5.0 wrong on arrival.

## [2026-07-31] CAL-UX.1 technical shape: scope-gated picker, settings-key selection, no schema change
**Context:** Live doc verification: Google `calendarList.list` is NOT authorized by the currently-granted `calendar.events.readonly` (needs `calendar.calendarlist.readonly`-class scope), but reading events from non-primary calendars IS already granted; Microsoft `Calendars.ReadBasic` covers both listing and per-calendar calendarView. Recon: accounts are encrypted blobs replaced wholesale on reconnect; cached events dedupe on `${provider}:${eventId}`; the poller's replace-except-linked purge self-corrects the cache when selection shrinks.
**Decision:** (1) Google connect flow adds `calendar.calendarlist.readonly` (same sensitive-not-restricted class — no new verification tier); pre-picker tokens are detected via the stored granted-scope string (undefined ⇒ stale) and degrade to a one-time Reconnect prompt, never an error, with NO network call attempted. (2) Selection lives in a plain settings key `calendar:{provider}:selectedCalendars` — deliberately OUTSIDE the auth blob so reconnect can never wipe it; absent key = provider default (primary), empty array forbidden at the API edge. (3) NO calendar_id column / migration: purge semantics already evict deselected calendars' events, and per-calendar attribution is not required by any current surface. (4) No pagination; honest per-calendar cap raised 50→250 and documented.
**Alternatives:** `calendar.readonly` broad scope (rejected — wider than needed); storing selection inside the auth blob (rejected — persistCalendarConnection replaces it wholesale); calendar_id column now (rejected — YAGNI, revisit if per-calendar colors/attribution ever land); OData/nextPageToken pagination (rejected — 250/calendar over 7 days is far beyond real usage; silent-cap risk documented in code instead).
**Rationale:** Smallest change that fully delivers "whatever the user selects"; every scope claim verified against official docs before design rather than assumed.

## [2026-07-31] CAL-UX.2 scoped for 2.6.0 — three agenda views, event modal, deterministic context with opt-in AI
**Context:** User wants the agenda to read as a real week ("full view of the week by default"), each meeting clickable into a Brain-style details modal, and cross-meeting context between recurrences ("mention things from the previous meeting if it's the same one") without overload. v2.5.0 was mid-smoke with a verified draft.
**Decision:** (1) Ship v2.5.0 first — CAL-UX.2 targets 2.6.0; the tag must pin `38a709c` (the draft's build commit) so later main commits can't leak into the release. (2) THREE agenda views behind a persisted switcher — list (current), week day-columns board (default), Outlook-style hour timeline — user's explicit call: "give options... most users will probably use outlook too". (3) Modal context = instant deterministic lookups always (same-series last session brief snippet + open action items + attendee→Brain-person fact matches) with an AI prep note generated ONLY on button click via the local-first per-task routing; in-memory cache per event. (4) Calendar descriptions stay out permanently — metadata-only policy reaffirmed; all modal context is LifeDash-derived.
**Alternatives:** Single week view replacing the list (rejected by user — wants options); auto-generated background prep notes (rejected — the overload the user explicitly doesn't want); relaxing metadata-only to fetch event bodies (rejected — core privacy differentiator, SPEC-anchored); folding CAL-UX.2 into 2.5.0 (rejected — release already large, scope-creep risk).
**Rationale:** The switcher resolves the layout question permanently instead of guessing one form; deterministic-first context is instant and free, matching the "efficient, not overloaded" constraint, while the opt-in note keeps AI value available exactly when wanted.

## [2026-07-31] CAL-UX.2b — metadata-only calendar policy AMENDED: descriptions stored locally; prep note may read them; attendee emails displayed in-modal
**Context:** User asked for the invite description and a full attendee list in the event details modal. This collided with the Phase G user decision (event bodies/descriptions NEVER persisted — SPEC-anchored, schema-enforced, and stated in the 2.5.0 release notes). The conflict was surfaced explicitly with the practical consequences before any code.
**Decision (all explicit user choices):** (1) Descriptions are fetched and STORED LOCALLY — plain-texted (HTML stripped, entities decoded amp-last), capped 4000 chars, migration `0045_silky_morg`; calendar data remains outside sync entirely (verified: no calendar table in syncConfig). (2) The on-demand prep note MAY include the description (≤1000-char slice inside the existing 4000-char context budget); attendee EMAILS still never enter any AI prompt — unchanged. (3) The modal displays the full attendee list including locally-stored emails. (4) Microsoft scope upgraded `Calendars.ReadBasic` → `Calendars.Read` (ReadBasic cannot return bodies); legacy tokens degrade to a byte-identical body-less query (word-boundary scope check — ReadBasic/ReadWrite do NOT satisfy it) so existing connections keep working, descriptions appearing after a one-time reconnect. Google needs no scope change (events scope already carries description). Locations/attachments/raw HTML remain forbidden. SPEC requirement retitled "read-only and local-only" with the amendment dated inline; 2.6.0 release notes state the storage honestly.
**Alternatives:** Fetch-on-open transient display (rejected by user — slower modal, provider-online dependency, Google needs calendar probing since no calendar_id column); display-only AI hygiene for descriptions (offered as recommendation, user chose prep-note access — agendas in invite bodies make better briefings); keeping the policy (offered, declined).
**Rationale:** The product owner deliberately traded a narrower privacy claim for utility, with the trade named before deciding, storage still device-local and unsynced, and the release-notes privacy copy corrected so the public claim matches the code.

## [2026-07-31] License switch: AGPL-3.0 → PolyForm Noncommercial 1.0.0 (first release: 2.6.0)
**Context:** User wants businesses to come to them for commercial use — "either by buying my solution or just asking me if they can." AGPL permits resale (with source disclosure) and legally allows stripping the Pro license gate; only a noncommercial license routes ALL commercial use through the author. Verified before switching: sole copyright holder (both git identities are the author — no contributor consents needed); zero copyleft production dependencies (license-checker: the only AGPL entry was lifedash itself; everything else MIT/Apache/ISC/BSD/OFL/BlueOak/Python-2.0/dual-MPL); license text taken VERBATIM from the canonical polyformproject/polyform-licenses repo (never paraphrased).
**Decision:** PolyForm Noncommercial 1.0.0 for all versions after v2.5.0. LICENSE = notice header (copyright, commercial-contact riegerdaniel@ymail.com, Required Notice line, AGPL-history note) + verbatim license text. package.json/lock `PolyForm-Noncommercial-1.0.0` (valid SPDX id). All "open source" claims rewritten to "source available / free for personal use": README (tagline, badge, comparison row, privacy bullet, License section), PRIVACY.md, CONTRIBUTING.md (now also grants the maintainer relicensing rights over contributions — the enabler for selling commercial licenses over a codebase with outside contributions), demo storyboard/overlay, social preview. Releases ≤ v2.5.0 remain AGPL forever (stated in LICENSE + README).
**Alternatives:** ELv2 (recommended by Claude — permits internal commercial use, protects license keys; rejected by user: they want businesses to have to ask); FSL (delayed-open-source goodwill; same rejection); dual AGPL+commercial (doesn't stop resale of the AGPL copy); staying AGPL (weakest against the stated goal).
**Rationale:** PolyForm NC is the only mainstream option matching the stated business model exactly: individuals/charities/edu/government free, every company a conversation. Known accepted costs, named before deciding: not OSI open source (marketing label change executed in the same commit); Certum OSS signing cert eligibility lost (ISSUES.md D4 amended, ~€200/yr more for Windows signing); pre-switch releases remain forkable under AGPL (low risk at current scale — argument for switching now rather than later).
