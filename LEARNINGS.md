# Project Learnings (Auto-Captured)

> Auto-generated by NEXUS during plan execution. Entries are created when tasks fail
> verification or require model escalation. These learnings are injected into future
> task agents to prevent repeating the same mistakes.
>
> Manual entries welcome — use the same format below.

---

## 2026-02-28 | execution-stop | installer/lifedash.iss, scripts/build-installer.js
**Issue:** Inno Setup .iss paths resolved relative to the .iss file location, not the project root
**Context:** Plan I.1 Task 1 — build-installer.js compiled the .iss but ISCC.exe couldn't find src\assets\icon.ico
**Fix:** Add `SourceDir=..` to [Setup] section so all paths resolve from the project root
**Tags:** inno-setup, build, paths

## 2026-02-28 | execution-stop | installer/lifedash.iss
**Issue:** `Flags: checked` is not a valid flag in Inno Setup's [Tasks] section
**Context:** Plan I.1 Task 1 — ISCC.exe rejected the .iss file at compile time
**Fix:** Remove the flag entirely — tasks are checked by default in Inno Setup
**Tags:** inno-setup, build, syntax

## 2026-02-28 | execution-stop | scripts/build-installer.js
**Issue:** winget installs Inno Setup to %LOCALAPPDATA%\Programs\ instead of Program Files
**Context:** Plan I.1 — ISCC.exe not found after winget install
**Fix:** Add %LOCALAPPDATA%\Programs\Inno Setup 6\ to the ISCC.exe search candidates
**Tags:** inno-setup, build, windows, paths

## 2026-02-28 | execution-stop | scripts/upload-release.js
**Issue:** GITHUB_TOKEN works for api.github.com but fails on uploads.github.com in bash
**Context:** v2.0.5 release — curl upload returned "Bad credentials" despite valid token
**Fix:** Used PowerShell Invoke-RestMethod as workaround; installed gh CLI for future releases
**Tags:** github, release, windows, auth

## 2026-02-28 | decision | src/main/autoUpdater.ts
**Issue:** SmartScreen blocks unsigned .exe installers downloaded via browser
**Context:** v2.0.5 release — installer triggered "Windows protected your PC" dialog
**Fix:** Distribute as ZIP (no SmartScreen on archives). Keep Setup.exe for auto-updater only (downloaded programmatically via net.request, no MOTW = no SmartScreen)
**Tags:** smartscreen, distribution, windows, security
