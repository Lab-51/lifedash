// === FILE PURPOSE ===
// Sessions Home page — the app's default route (V3.1 session-centric pivot).

import SessionsHome from '../components/SessionsHome';

export default function SessionsHomePage() {
  return <SessionsHome />;
}
