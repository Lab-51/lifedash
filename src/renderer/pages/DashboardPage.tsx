// === FILE PURPOSE ===
// Home dashboard page — renders the DashboardModern component.

import DashboardModern from '../components/DashboardModern';

export default function DashboardPage() {
  return <DashboardModern />;
}
