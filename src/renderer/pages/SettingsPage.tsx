// === FILE PURPOSE ===
// Settings page — app configuration, AI providers, and preferences.

import SettingsPageModern from '../components/SettingsPageModern';

export default function SettingsPage() {
  return <SettingsPageModern />;
}
